# Autopilot decision log

Decisions Claude made without waiting for the user, newest last. Reverse anything you disagree with.

- 2026-08-16 — Entered autopilot. Standing plan: (1) finish UI polish from the two client-survey agents,
  (2) keep the t1 game going as P1 whenever the user moves, (3) run a small multi-seed agent evaluation
  (3 games, seats swapped) once the UI round is done, (4) keep tests green and commit after each step.
- 2026-08-16 — Launched eval1 (seed 3001: P0 Yugi beatdown vs P1 Kaiba control) and eval2 (seed 3002: P0 Kaiba
  control vs P1 Yugi beatdown) with Opus agents, 120-play cap each. eval2's seat-1 agent hit the concurrent-
  subagent limit; will launch it as soon as a slot frees (its P0 agent just waits meanwhile).
- 2026-08-16 — Sleeves: per-player card backs chosen by player label, stored front-end only in
  web/data/sleeves.json (not in duel records), picker in the header for your own seat.
- 2026-08-16 16:56 — Test-summoned a headless Claude bot on duel bot1 seat 1 to validate the Summon Claude path (costs one claude -p run).
- 2026-08-16 17:09 — BrowserPod runtime is proprietary + API-keyed; user refuses their API. Default chosen: vendor-free local Claude terminal (node-pty + xterm.js, NO tmux) as the talkable bot; BrowserPod tab left dormant (needs a key). Awaiting user preference for 60s first.
- 2026-08-16 — Eval results (2 games, seats+strategies swapped): eval1 P1 Kaiba-control beat P0 Yugi-beatdown (T16, Judge Man
  lethal; P0's Card Destruction dumped Curse of Dragon which P1 Reborn'd); eval2 P1 Yugi-beatdown beat P0 Kaiba-control
  (T14 4-monster alpha strike after Soul Exchange stole Rogue Doll). Both P1 wins; sample too small to conclude anything.
  Agent CLI notes to act on: (a) `play` should honor --auto-pass (post-play respond? prompts cost ~15/48 calls);
  (b) `wait` should print only the delta since the previous decision by default; (c) menu label "Select the card(s)
  to destroy" for Trap Master's non-destroying reveal is the core's hint string, not ours; (d) never batch `play`
  calls with && (menu numbering shifts) — add to PLAYER.md; (e) Book of Secret Arts offered with no valid target
  (core lists it) — note as legal-but-useless in PLAYER.md.
