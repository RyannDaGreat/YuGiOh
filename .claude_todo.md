# YuGi — TODO

Project: LLM-playable Yu-Gi-Oh. Headless duel engine on ocgcore-wasm, CLI-first,
Svelte UI as a client. Full text state + greppable offline card DB.
See README.md for architecture; this file is the running task list.

## ACTIVE WORKSTREAM — visual overlay layer (animations) [started 2026-08-16]
Goal (user): unify card MOVEMENT into one animated primitive (any zone → any zone,
flip mid-flight), smooth hand/zone reflow, life-point tween + anime tick/settle sound,
and dashed relationship lines for equips. Principle: animation = interpolate the DELTA
between state N and N+1; NOT a per-transition zoo. Reuses the existing dagger/float
overlay + `centerOf(data-zone)` anchors + `play(ev)` dispatcher in Table.svelte.
See claude_instructions.md §11. Divvy: the tightly-coupled Table.svelte work is MINE
(serial, no merge conflicts on the shared file); the independent slow chunk (LP anime
sound sourcing + a self-contained LPCounter) is one worktree agent.

- [x] P1 Foundation: `rectOf`/`anchorRect` (reuse `centerOf`); overlay lists for flyers + `<RelationLines>`; app.css vars
- [x] P1 Backend: src/events.js emits generic `move {from,to,name,code,faceFrom,faceTo,reason}` on every zone-changing T.MOVE (+ 2 unit tests)
- [x] P1 Backend: src/state.js adds `equipTarget: coord(card.equipCard)` beside `equippedTo`
- [x] P2 FlyingCard.svelte + play() case: one flyer per zone→zone move (mid-flight scaleX flip on reveal/hide); draw = N staggered deck→hand flyers; snap-on-jump guard (fly only on small/live bursts). Verified live.
- [x] P3 Hand reflow: card-following keys (`code#occurrence`) + Svelte `animate:flip`; neighbours slide
- [x] P4 RelationLines.svelte: dashed marching equip lines, board+resize reactive. Verified live (eval2 move 8: Sword of Dark Destruction → Dragon Zombie).
- [x] P5 [AGENT worktree-agent-a979f33fe01a8050e, merged] LPCounter.svelte + lptick/lpsettle cues. NOTE: Dueling Nexus has no LP-tick loop and rips are copyrighted, so the agent used CC0 Kenney blips (functional, not the authentic anime sound). Swap to a real anime rip later if the user wants (personal-use, vendor/).
- [x] P5b Wired <LPCounter value={lp} {sound}/> into Table.svelte; LP tweens on change. Verified live (7774→6700 mid-tween).
- [x] Verify: node --test 45/45, svelte-check 0 warnings (38 pre-existing type-lint errors only), flyer/equip-line/LP-tween all screenshotted, no console errors.
- [ ] FINAL: restart user's :5178 clean; notify user it's playable.

## Done
- [x] Phase 0 spike: ocgcore-wasm@0.1.2 runs headless in Node 24 (sync build), core v11.0
- [x] Vendored Project Ignis CardScripts + BabelCDB (pinned in setup.sh) + strings.conf
- [x] Verified 50-card SDY/SDK decklists (Yugipedia + YGOPRODeck); all names resolve
- [x] Seeded deck shuffle (rng.js); duel = seed + decks + responses, replayed per command
- [x] Lua script errors are fatal (no silent vanilla cards)
- [x] WIN is terminal; RETRY is a loud error; empty SELECT_CHAIN auto-declined
- [x] view.js: per-player masking ported from single_duel.cpp / EDOPro generic_duel.cpp
- [x] field.js: client-side field model from the masked stream
- [x] log.js: YGN log renderer
- [x] state.js: full state (core query + masking + viewer memory), unseen-card pools
- [x] menu.js: every SELECT_*/ANNOUNCE_* -> numbered menu -> OcgResponse
- [x] strings.js: description decode (>>20 rule), system/victory strings
- [x] session.js + bin/ygo.js: new/state/log/menu/play/undo/list/card/search/deck/decks
- [x] test/consistency.test.js: model vs masked core query + leak detection, 3 seeds x 250 steps
- [x] Played a real duel through the CLI as both sides (Man-Eater Bug flip, chain, targets)
- [x] README.md

- [x] Svelte UI (web/): board, hand, log pane, menu buttons, card text; polls; Puppeteer-verified
- [x] `ygo dump-cards` -> vendor/cards.txt (one line per card), in setup.sh
- [x] `ygo wait` (block until it is your decision) + PLAYER.md seat instructions
- [x] Duel records freeze passcodes at creation (replay-stable across DB changes)
- [x] cards.cdb bigint columns (setcode/race) read as text — fixed a latent cardReader crash
- [x] Duplicate "Activate X" menu labels get (effect #n)
- [x] Subagent-vs-subagent match (match1, seed 2024): full 14-turn game, P1 Kaiba wins
- [x] Agent usability notes folded in: wait --auto-pass/--ask-for/--ask-at, fresh chain-prompt
      context (turn/phase from field model), tribute hints on summon labels, negative-LP wrap
      fixed, DUEL OVER on state header, "End turn (skip Main Phase 2)", PLAYER.md guidance
- [x] `ygo brief` (full agent prompt), `ygo tally`, strategies/*.md
- [x] test/menu.test.js unit tests

- [x] Playback (--at / slider) and fork-at-move, CLI + web
- [x] Pretty web UI: card art (ygo fetch-pics, local /pics route), Tailwind table, hover preview,
      daggers/flashes/floating LP, WebAudio sfx, phase strip; isolated in web/src/lib/pretty/
- [x] Per-duel chat (table talk; never instructions): src/chat.js, ygo chat, web panel; PLAYER.md/HOST.md rule
- [x] Duels are permanent records on a clock: per-move `times` in the record (annotation, never replayed;
      old records read as null), chat cut off at the replayed move in playback, index duel history
      (finished section, created/last move/chat count, replay links), `ygo list` timestamps

- [x] Deck-library FOUNDATION: extended deck schema — category (structure|user), format (classic|goat),
      optional extra/side decks, per-deck `manual`. loadDeck validates names + card placement (Extra-Deck
      monsters must be in `extra`, not `main`) + sizes (goat main 40–60, extra/side ≤ 15); legacy {name,main}
      still loads. replayDuel({…, extraCodes, format}) deals extra cards into OcgLocation.EXTRA and builds
      MODE_GOAT for goat (else MR5, byte-identical for old records). Duel record + createDuel freeze
      format + extraCodes/sideCodes + manual; forkDuel copies them. `ygo new --format`, `ygo deck` (shows
      category/format/all piles/manual), `ygo decks` (grouped by category). viewDuel/web payload expose
      `format` + per-seat deck metadata; state header shows the GOAT tag. Example `src/decks/goat-sample.json`
      (40 main + 3 Fusions). Tests in test/decks.test.js. Deck identity = file name, never card contents.

## Next
- [ ] Deck-viewer UI + full deck library (separate wave): render category/format/manual + extra/side piles,
      home-screen deck selector, deck editor. The engine/payload already expose what it needs (view.decks,
      view.format, engine.duelPayload deckMeta/format).
- [ ] Multi-seed evaluation: N duels x 2 strategies with swapped seats; tally (agents launched by Claude Code)
- [ ] Optional: expose MSG_MOVE reason (fork binding or query REASON) so log says "tributed"/"destroyed"
- [ ] Optional: token-authenticated daemon if the honor-system boundary ever matters
- [ ] Optional: more decks (e.g. a modern Blue-Eyes list) — anything in cards.cdb works today

## Housekeeping
- [x] setup.sh recreates vendor/ + npm deps (root + web); vendor/ and node_modules/ gitignored
- [x] Committed (3 commits so far)

## Deck library (requested decks to author, research-sourced manuals)
- [x] AUTHORED — 16 selectable structure decks under src/decks/ from docs/goat-decks.md, every card
      name validated via codeOf (loud on typo) and placement via isExtraDeckCard; all pass loadDeck;
      `ygo fetch-pics` + extra-deck art fetch leave every card with an image in vendor/pics (0 404s);
      `npm test` green (43/43). Manuals copied from the doc (combo-lines-first, weaknesses small,
      [CDB]/[YP-GOAT]/[SDP]/⚠ citations preserved). docs/goat-decks.md also copied into the worktree.
  - [x] 10 GOAT meta decks (format goat, 40 main each): goat-control (+6 extra, +8 side),
        warrior-toolbox (+5 extra), chaos-turbo (+4 extra), zombie-zoo, reasoning-gate, stall-burn,
        empty-jar, cyber-stein (+5 extra, +6 side), water-umi, earth-beatdown (+4 extra)
  - [x] Theme/era decks: blue-eyes-dragons (classic, 40), dark-magician (classic, 40),
        relinquished-ter (goat, 40, +3 extra), exodia-stall (goat, 40)
  - [x] pegasus-structure-deck (classic, exact Starter Deck: Pegasus / SDP, 50 cards, one of each)
  - [x] pegasus-goat (goat, 48 — SDP minus Monster Reborn + Witch of the Black Forest, the doc's GOAT-legal build)
  - [~] SKIPPED per doc: Last Turn FTK (§3.5) — doc marks the shell as ⚠ unverified / novelty, not a tuned list
  - Note: (GOAT) errata aliases (goat-entries.cdb) intentionally NOT referenced — canonical names used;
    the sim applies GOAT rulings via MODE_GOAT. `fetch-pics` only pulls MAIN art, so Extra-Deck-only
    fusions were fetched separately (pre-existing CLI gap, not changed here).

## ACTIVE — UX/bugs batch + modern curated decks [2026-08-17]
Bugs (front-end, mine):
- [ ] Animation glitch: card leaving hand flies HUGE (flyer sized to full-width hand-area fallback). Fix: fixed card size at anchor CENTER, not the anchor's width. VLM slow-mo verify (user saw Terraforming balloon before GY in a Pendulum-vs-Spellcaster duel).
- [ ] Scrubber UX: bar must be a fixed big size; "live/move X/Y" + "new id" must NOT resize/jitter as you drag to the end. Gray out "new id". At the end show "move N/N" not "N-1/N". Static layout.
- [ ] Deck selection UX: "Play this deck" always sets P0 even when reached via the P1 picker. Rethink: clicking a seat's deck should open a picker (list) and choosing one sets THAT seat. Add: set two AIs vs each other; choose to go 2nd (which seat is P0).
- [ ] Presence: P1 shows offline with no way back online; heartbeat/online logic unreliable. Investigate + fix. Also HOST.md: runserver.sh host Claude did NOT auto-pick-up the game (user had to tell it to look) — must be automatic.
Decks (research + author, curated, best-possible manuals; validate every card vs cards.cdb, report missing modern cards):
- [ ] Endymion — named after YouTuber "Shadow Spectre"; from masterduelmeta Jan-2026 EHOV2 list; DETAILED manual (lots of lines). Link: https://www.masterduelmeta.com/top-decks/master-v/january-2026/endymion/shadow-spector/ehov2
- [ ] Sky Striker — named after a famous Sky Striker YouTuber (research who).
- [ ] Sushi boat deck — research what it is, author it.
- [ ] HOST architecture idea (user): on entering/creating a table, auto-spawn a background agent PER GAME so one Claude session plays multiple games (fixes "which table" confusion + no manual "go look"). Scope with presence/host fix.
