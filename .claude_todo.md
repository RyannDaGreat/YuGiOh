# TODO — investigate & fix DeepSeek chat issues (2026-08-19)

## Spell / Trap presentation audit (2026-08-22)

- [x] Independently verify the general Spell/Trap activation and chain lifecycle against Konami sources.
- [x] Derive a renderer semantic state machine and micro-step contract in `.frenzy/spell-rules-audit.md`.
- [x] Confirm whether the existing ocgcore message stream exposes the required ordering; no production code changed.
- [x] Create a black-box Upstart Goblin observation oracle and compare it with existing fixtures/presentation code in `.frenzy/upstart-observation-oracle.md`.

Symptoms from duel zombie-madness-vs-spellcasters-command-2 chat (deepseek-v4-pro at P1):
(a) last reply cut off MID-WORD ("...wiped it any") — truncation
(b) "amnesia": each reply restarts from scratch, ignores its own prior answers
(c) surface-deep: answers "what" (re-narrates log) when asked "why"

- [x] 1. Git pull (up to date), read manifest §24 + src/ai/chat.js + player.js
- [x] 2. Reproduce: no local traces (static-host duel), reproduced both bugs offline with a
       scripted provider — (a) the posted line was exactly 280 = MAX_REPLY_CHARS chars;
       (b) the follow-up prompt provably lacked the seat's own previous answer
- [x] 3. Fix (a): capReply (tidyTruncated at the cap, never mid-word); MAX_REPLY_CHARS
       280 -> 500, tied to chat.js MAX_CHAT_CHARS (600 would have made appendChat throw)
- [x] 4. Fix (b): earlier-context keeps own lines regardless of the cursor (`at <= since ||
       m.seat === seat`), marked "(you)" in the prompt; EARLIER_LINES 8 -> 12
- [x] 5. Fix (c): chatPrompt — how/why wants REASONING not re-narration, up to three
       sentences when asked to explain, stay consistent with own lines / correct them,
       never state an unsure rule
- [x] 6. Tests: 2 new regression tests + extended cap test; npm test 149 pass / 1 skip / 0 fail
- [x] 7. claude_instructions.md §24 updated; concerns.md entry appended
- [x] 8. Commit

## Independent spell/effect backend audit (2026-08-22)

- [x] Trace core messages through masking, event extraction, payload snapshots, beat scheduling, and move scrubber
- [x] Identify authoritative data retained and semantic causality/order lost by the animation digest
- [x] Design deterministic reversible semantic-step navigation and a shared animation clock/speed control
- [x] Record the audit in `.frenzy/spell-backend-audit.md` and commit it without implementing behavior

## "always treated as Umi" broken in the browser host (2026-08-23)

Symptom (duel fury-from-the-deep-vs-fury-from-the-deep-3, web UI, P0): Levia-Dragon -
Daedalus's ignition effect is not offered although A Legendary Ocean is face-up.

- [x] 1. Rule check: A Legendary Ocean IS a legal "Umi" to send; nothing on the field stops it
- [x] 2. Ignis implements "always treated as Umi" via `datas.alias` in cards.cdb, not Lua
- [x] 3. Synthetic board probe: Node host offers the effect for BOTH Umi and A Legendary Ocean
- [x] 4. Root cause: bin/bake-carddata.js `bakedCard` reads `info.alias`, but `cardInfo()`
       never returns alias -> EVERY card in the browser bundle bakes with alias 0
- [x] 5. Causation proved: zeroing alias in the card reader reproduces exactly (ALO false, Umi true)
- [x] 6. Fix: one shared `bakedCard` (src/cardsource-browser.js, beside its inverse `bakedRow`),
       alias/setcode/strings from the raw cdb row; bin + test both call it
- [x] 7. Regression tests: bakedRow(bakedCard(code)) == cards.cdb over every core-read field, for
       every card in the committed bundle; plus test/browser-host-rules.test.js — browser source
       only, real core, Daedalus's cost sees A Legendary Ocean and Bugroth may attack directly.
       Both confirmed to FAIL against the shipped bundle.
- [x] 8. Rebaked web/static/carddata (11 alias fixes + 19 stale descs) and .assets/carddata/
       cards-all.json (415 aliases restored, NOT pushed — bin/publish-assets.sh does that)
- [x] 9. npm test 151 pass / 1 skip / 0 fail; claude_instructions.md §19 + new §26; concerns.md entry
- [ ] 10. Owner's call: commit + push main (Pages auto-deploys), and run bin/publish-assets.sh
- [ ] 11. WARNING to carry: recorded responses are INDEXES into the core's activate list, and the fix
       inserts entries into it (verified: [Pot of Greed] -> [Daedalus, Pot of Greed]). Browser duels
       recorded under the bug can replay as a DIFFERENT game — the fury-from-the-deep-3 duel almost
       certainly will (Salvage was activated at turn 9 with Daedalus already face-up).
