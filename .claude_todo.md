# YuGi — TODO

Project: LLM-playable Yu-Gi-Oh. Headless duel engine on ocgcore-wasm, CLI-first,
Svelte UI as a client. Full text state + greppable offline card DB.
See README.md for architecture; this file is the running task list.

## Done
- [x] Phase 0 spike: ocgcore-wasm@0.1.2 runs headless in Node 24 (sync build), core v11.0
- [x] Vendored Project Ignis CardScripts + BabelCDB (pinned in setup.sh) + strings.conf
- [x] Verified 50-card SDY/SDK decklists (Yugipedia + YGOPRODeck); all names resolve
- [x] Seeded deck shuffle (rng.js); duel = seed + decks + responses, replayed per command
- [x] Lua script errors are fatal (no silent vanilla cards)
- [x] WIN is terminal; RETRY is a loud error; empty SELECT_CHAIN auto-declined
- [x] view.js: per-player masking ported from single_duel.cpp / EDOPro generic_duel.cpp
- [x] field.js: client-side field model from the masked stream
- [x] log.js: YGN log renderer
- [x] state.js: full state (core query + masking + viewer memory), unseen-card pools
- [x] menu.js: every SELECT_*/ANNOUNCE_* -> numbered menu -> OcgResponse
- [x] strings.js: description decode (>>20 rule), system/victory strings
- [x] session.js + bin/ygo.js: new/state/log/menu/play/undo/list/card/search/deck/decks
- [x] test/consistency.test.js: model vs masked core query + leak detection, 3 seeds x 250 steps
- [x] Played a real duel through the CLI as both sides (Man-Eater Bug flip, chain, targets)
- [x] README.md

- [x] Svelte UI (web/): board, hand, log pane, menu buttons, card text; polls; Puppeteer-verified
- [x] `ygo dump-cards` -> vendor/cards.txt (one line per card), in setup.sh
- [x] `ygo wait` (block until it is your decision) + PLAYER.md seat instructions
- [x] Duel records freeze passcodes at creation (replay-stable across DB changes)
- [x] cards.cdb bigint columns (setcode/race) read as text — fixed a latent cardReader crash
- [x] Duplicate "Activate X" menu labels get (effect #n)
- [x] Subagent-vs-subagent match (match1, seed 2024): full 14-turn game, P1 Kaiba wins
- [x] Agent usability notes folded in: wait --auto-pass/--ask-for/--ask-at, fresh chain-prompt
      context (turn/phase from field model), tribute hints on summon labels, negative-LP wrap
      fixed, DUEL OVER on state header, "End turn (skip Main Phase 2)", PLAYER.md guidance
- [x] `ygo brief` (full agent prompt), `ygo tally`, strategies/*.md
- [x] test/menu.test.js unit tests

- [x] Playback (--at / slider) and fork-at-move, CLI + web
- [x] Pretty web UI: card art (ygo fetch-pics, local /pics route), Tailwind table, hover preview,
      daggers/flashes/floating LP, WebAudio sfx, phase strip; isolated in web/src/lib/pretty/
- [x] Per-duel chat (table talk; never instructions): src/chat.js, ygo chat, web panel; PLAYER.md/HOST.md rule
- [x] Duels are permanent records on a clock: per-move `times` in the record (annotation, never replayed;
      old records read as null), chat cut off at the replayed move in playback, index duel history
      (finished section, created/last move/chat count, replay links), `ygo list` timestamps

- [x] Deck-library FOUNDATION: extended deck schema — category (structure|user), format (classic|goat),
      optional extra/side decks, per-deck `manual`. loadDeck validates names + card placement (Extra-Deck
      monsters must be in `extra`, not `main`) + sizes (goat main 40–60, extra/side ≤ 15); legacy {name,main}
      still loads. replayDuel({…, extraCodes, format}) deals extra cards into OcgLocation.EXTRA and builds
      MODE_GOAT for goat (else MR5, byte-identical for old records). Duel record + createDuel freeze
      format + extraCodes/sideCodes + manual; forkDuel copies them. `ygo new --format`, `ygo deck` (shows
      category/format/all piles/manual), `ygo decks` (grouped by category). viewDuel/web payload expose
      `format` + per-seat deck metadata; state header shows the GOAT tag. Example `src/decks/goat-sample.json`
      (40 main + 3 Fusions). Tests in test/decks.test.js. Deck identity = file name, never card contents.

## Next
- [ ] Deck-viewer UI + full deck library (separate wave): render category/format/manual + extra/side piles,
      home-screen deck selector, deck editor. The engine/payload already expose what it needs (view.decks,
      view.format, engine.duelPayload deckMeta/format).
- [ ] Multi-seed evaluation: N duels x 2 strategies with swapped seats; tally (agents launched by Claude Code)
- [ ] Optional: expose MSG_MOVE reason (fork binding or query REASON) so log says "tributed"/"destroyed"
- [ ] Optional: token-authenticated daemon if the honor-system boundary ever matters
- [ ] Optional: more decks (e.g. a modern Blue-Eyes list) — anything in cards.cdb works today

## Housekeeping
- [x] setup.sh recreates vendor/ + npm deps (root + web); vendor/ and node_modules/ gitignored
- [x] Committed (3 commits so far)

## Deck library (requested decks to author, research-sourced manuals)
- [ ] 10 GOAT meta decks (Goat Control, Warrior Toolbox, Chaos Turbo, Reasoning Gate, Burn, Empty Jar, Stein OTK, Water/Umi, etc.) — docs/goat-decks.md
- [ ] Theme/era decks: Umi/Water, Dragon (Blue-Eyes/Lord of D.), Dark Magician, Relinquished/Thousand-Eyes, Exodia stall
- [ ] PEGASUS deck: the EXACT contents of the official Structure Deck: Pegasus (Pegasus J. Crawford), verbatim from Yugipedia — not a reconstruction (user)
- [ ] Each deck: manual = combo-lines-first (biggest), weaknesses (small), research-sourced + cited; unique name per manual variant
